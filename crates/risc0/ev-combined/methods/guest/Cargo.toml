[package]
name = "ev_combined"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^3.0.3", default-features = false, features = [
    'std',
] }
ev-zkevm-types = { path = "../../../../ev-zkevm-types" }
